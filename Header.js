import React from "react";
import classes from '../Header/Header.module.css';

const Header = ()=>{
    return(
        <header className={classes['header']}>
            <h1>Login Front Page</h1>
        </header>
    );
};
export default Header;